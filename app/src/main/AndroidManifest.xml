<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.maci.foxconn.isfcandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoActionFullScreenTheme"

        android:usesCleartextTraffic="true">
        <!--<activity android:name=".MainActivity">-->
        <!--<intent-filter>-->
        <!--<action android:name="android.intent.action.MAIN" />-->
        <!--<category android:name="android.intent.category.LAUNCHER" />-->
        <!--</intent-filter>-->
        <!--</activity>-->

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity" />

        <activity android:name=".HomeActivity" />

        <activity android:name=".TitleBarActivity" />

        <activity android:name=".InStorageWorkOrderActivity" />

        <activity android:name=".InStorageDetailInfoActivity" />

        <activity android:name=".InStorageDetailInfoActivity_1"/>

        <activity android:name=".UserActivity" />

        <activity android:name=".InStorageActivity" />

        <activity android:name=".OutStorageWorkOrderActivity" />

        <activity android:name=".AsnRelatedActivity" />

        <activity android:name=".HoneyWellScannerActivity"/>

    </application>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
         
    <uses-permission android:name="android.permission.INTERNET" />
         
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.honeywell.decode.permission.DECODE" />

</manifest>